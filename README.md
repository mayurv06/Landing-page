Vibe Travel — Landing Page (Travel Booking Theme)

Preview

- Open `index.html` in a browser, or run a local server in the project folder:

```bash
# Python 3
python -m http.server 8000
# then open http://localhost:8000
```

Files

- `index.html` — travel-focused markup with hero, features, how-it-works, and testimonials
- `styles.css` — travel palette, glassmorphism cards, responsive layout
- `script.js` — lightweight particle background and small CTA interactions

Notes

- Mobile-first, accessible contrast, and lightweight JS.
- Images are loaded from unsplash for quick preview; swap them with your assets folder for production.
- Want this converted to React + Tailwind or hooked to a booking backend? Tell me which and I'll scaffold it.